<%= form_for @post do |f| %>
	<%= render 'common/form_errors', obj: @post %>
	<p>
		<%= f.label :title %>
		<%= f.text_field :title %>
	</p>
	<p>
		<%= f.label :body %>
		<%= f.text_area :body %>
	</p>
	<p>
		<%= f.button 'Submit', class: 'button' %>
	</p>
<% end %>